'use client'

import { TopHeader } from '@components/TopHeader'

import { Container } from './styles'

export const Header = () => {
  return (
    <Container>
      <TopHeader module="institution" />
    </Container>
  )
}
