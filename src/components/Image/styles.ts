import styled from 'styled-components'
import { PlaceHolder } from '../styled'

export const LoadingContainer = styled(PlaceHolder)`
  width: 100%;
  height: auto;
  border-radius: inherit;
  -webkit-border-radius: inherit;
`
